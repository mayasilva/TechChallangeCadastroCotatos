#nome do workflow
name: Workflow TechChallange

#defini quando o workflow será disparado
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
   build:
    name: .Net Build
    runs-on: ubuntu-latest
    steps:
    #marketplace - Setup .NET Core SDK
    - uses: actions/setup-dotnet@v4.0.0
    
    - uses: actions/checkout@v4
    - name: Setup dotnet
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    - name: Install dependencies
      run: dotnet restore ./TechChallangeCadastroCotatos/TechChallangeCadastroContatosAPI.csproj
    - name: Build
      run: dotnet build .\TechChallangeCadastroCotatos\TechChallangeCadastroContatosAPI.csproj --no-restore

    
